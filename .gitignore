# ----------------------------------------------------------------------------
# Python virtual environment
venv/

# Byte-compiled Python files
*.pyc
*.pyo
__pycache__/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Logs and temporary files
*.log
*.tmp

# ----------------------------------------------------------------------------
# Keep the 'data/Ally', 'data/IB', and 'data/cleaned' directories in Git,
# but ignore any other files placed there. We'll allow .gitkeep so the
# directories remain tracked, even when empty.
# ----------------------------------------------------------------------------

# Ignore everything in data/
data/*

# But don't ignore these subfolders themselves
!data/Ally/
!data/IB/
!data/cleaned/

# Now ignore everything inside each subfolder
data/Ally/*
data/IB/*
data/cleaned/*

# Except for .gitkeep files - we want those tracked
!data/Ally/.gitkeep
!data/IB/.gitkeep
!data/cleaned/.gitkeep
